version: '3.3'

services:
  server:
    image: molero/bootcamp-backend:main-8
    container_name: myapp-node-server
    command: /usr/src/app/node_modules/.bin/nodemon server.js
    depends_on:
      - mongo
    environment:
      - MONGO_HOSTNAME=mongo
      - MONGO_DB=myapp_db
      - MONGO_PORT=27017

  mongo:
    image: mongo:4.4.18
    ports:
      - "27017:27017"

  client:
    image: molero/bootcamp-frontend:main-8
    stdin_open: true
    container_name: myapp-react-client
    depends_on:
      - server
    ports:
      - "3001:3000"

